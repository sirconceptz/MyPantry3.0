<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyPantry" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_900</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_500</item>
        <item name="colorSecondaryVariant">@color/blue_900</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimary</item>
        <!-- Cardview background color. -->
        <item name="cardBackgroundColor">@color/blue_900</item>
        <!-- Background color. -->
        <item name="background">@color/blue_900_plus</item>

    </style>

    <style name="Theme.MyPantry.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyPantry.AppBarOverlay" parent="ThemeOverlay.AppCompat.DayNight.ActionBar" />

    <style name="Theme.MyPantry.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight" />

    <!-- Divider line -->
    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
        <item name="android:layout_marginTop">@dimen/divider_margin</item>
        <item name="android:layout_marginHorizontal">@dimen/divider_sides_margin</item>
    </style>

    <style name="CustomEditText" parent="android:Widget.EditText">
        <item name="android:background">@drawable/round_edittext_night</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">4dp</item>
    </style>

    <style name="CustomButton" parent="android:Widget.Material.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>